{
  "name": "@student-claw/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@student-claw/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "bun-types": "^1.3.5",
    "typescript": "^5.9.3"
  }
}
