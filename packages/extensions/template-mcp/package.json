{
  "name": "@orbyt/template-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test src"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@orbyt/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "bun-types": "^1.3.5",
    "typescript": "^5.9.3"
  }
}
